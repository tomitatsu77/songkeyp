
<div class="jumbotron">
  <h4 class="display-5">メールアドレスの変更</h4>
  
  <hr class="my-4">
  <%= form_with model: @user, url: mypage_path ,local:true do |f| %>
  <h4>メールアドレス</h4>
    <%= f.text_field :email %>
    <!--<label for="inputImage">ProfileImage</label>-->
    <%#= f.file_field :profile_image, placeholder: "プロフィール画像", accept: "image/*" %> 
  <%= f.submit '更新' %>
<% end %>
<div class="mt-3"><%= link_to "戻る", mypage_path,class: "btn btn-outline-secondary" %></div>
</div>