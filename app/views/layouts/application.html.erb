<!DOCTYPE html>
<html>
  <head>
    <title>KaraokeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <% if user_signed_in? %>
          <%= link_to "曲一覧", songs_path,class: "navbar-brand" %>
          <%= link_to "アーティスト順", artists_path,class: "navbar-brand" %>
        <% else %>
          <%= link_to "そんぐキーぷ", root_path,class: "navbar-brand" %>
        <% end %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button><br>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
          <div class="navbar-nav">
            <% if user_signed_in? %>
              <%= link_to "マイページ", mypage_path,class: "nav-item nav-link" %>
              <%= link_to "そんぐキーぷとは？", about_path ,class: "nav-item nav-link"%>
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete,class: "nav-item nav-link" %>
            <% else %>
              <%= link_to "新規登録", new_user_registration_path,class: "nav-item nav-link" %>
              <%= link_to "ログイン", new_user_session_path,class: "nav-item nav-link" %>
              <%= link_to "そんぐキーぷとは？", about_path,class: "nav-item nav-link" %>
            <% end %>
          </div>
        </div>

      </nav>
    </header>
    <%= flash[:notice] %>
    <%= yield %>
  </body>
</html>
